import { InputText } from "./StyleInput";

export function InputProfile({
    editable,
    placeholder,
    
}) {
    return (
        <InputText
            placeholder={placeholder}
            editable={editable}
            
        />

    )
}